[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run start'

[variables]
NODE_ENV = 'production'
NEXT_TELEMETRY_DISABLED = '1'
NPM_CONFIG_LOGLEVEL = 'verbose'
